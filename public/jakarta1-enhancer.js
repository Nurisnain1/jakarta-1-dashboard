(()=>{
  const money=new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0});
  const number=new Intl.NumberFormat('id-ID');
  const cache=new Map(),pending=new Map(),feedbackCache=new Map();
  const txt=el=>String(el?.textContent||'').trim();
  const byHeading=label=>{const h=[...document.querySelectorAll('h2,h3')].find(el=>txt(el).toLowerCase()===label.toLowerCase());return h?.closest('div.space-y-5,section')||h?.parentElement||null};
  const currentPeriod=root=>{const ss=[...root.querySelectorAll('select')],year=ss.find(s=>/^(2025|2026)$/.test(s.value)),month=ss.find(s=>/^(0[1-9]|1[0-2])$/.test(s.value));return `${year?.value||'2026'}-${month?.value||'09'}`};
  async function audit(period){if(cache.has(period))return cache.get(period);if(pending.has(period))return pending.get(period);const p=fetch(`/api/jakarta1-staff-audit?period=${encodeURIComponent(period)}`,{cache:'no-store'}).then(r=>r.json()).then(j=>{const rows=j.rows||[];cache.set(period,rows);pending.delete(period);return rows}).catch(()=>{pending.delete(period);return[]});pending.set(period,p);return p}
  const mapRows=rows=>new Map(rows.map(r=>[`${r.store}|${String(r.name).trim().toUpperCase()}`,r]));
  const currencyValue=s=>{const raw=String(s||'').replace(/[^0-9-]/g,'');const v=Number(raw);return Number.isFinite(v)?v:0};

  function growth(){const h=[...document.querySelectorAll('h3')].find(x=>txt(x).startsWith('2025 vs 2026')),table=h?.closest('section')?.querySelector('table')||h?.parentElement?.parentElement?.querySelector('table');if(!table)return;for(const tr of table.querySelectorAll('tbody tr')){const cells=[...tr.querySelectorAll('td')],cell=cells[3];if(!cell||cell.dataset.j1Growth==='1')continue;const raw=txt(cell);if(raw==='—')continue;const v=Number(raw.replace('%','').replace(',','.').replace(/[^0-9.-]/g,''));if(!Number.isFinite(v))continue;cell.dataset.j1Growth='1';cell.classList.add('font-black');if(v>0){cell.classList.add('text-emerald-600');cell.textContent=`↑ ${raw}`}else if(v<0){cell.classList.add('text-rose-600');cell.textContent=`↓ ${raw}`}else{cell.classList.add('text-slate-500');cell.textContent=`→ ${raw}`}}
    const bodyRows=[...table.querySelectorAll('tbody tr')],v25=bodyRows.reduce((a,tr)=>a+currencyValue(txt(tr.querySelectorAll('td')[1])),0),v26=bodyRows.reduce((a,tr)=>a+currencyValue(txt(tr.querySelectorAll('td')[2])),0),g=v25?(v26-v25)/v25*100:0;let foot=table.querySelector('tfoot[data-j1-year-total="1"]');if(!foot){foot=document.createElement('tfoot');foot.dataset.j1YearTotal='1';table.append(foot)}foot.innerHTML=`<tr class="border-t-2 bg-slate-50 font-black"><td class="${bodyRows[0]?.querySelector('td')?.className||''}">GRAND TOTAL</td><td class="${bodyRows[0]?.querySelectorAll('td')[1]?.className||''}">${money.format(v25)}</td><td class="${bodyRows[0]?.querySelectorAll('td')[2]?.className||''}">${money.format(v26)}</td><td class="${bodyRows[0]?.querySelectorAll('td')[3]?.className||''}">${v25?(g>0?'↑ ':g<0?'↓ ':'→ ')+new Intl.NumberFormat('id-ID',{maximumFractionDigits:1}).format(g)+'%':'—'}</td></tr>`;
  }

  function productFocus(){const h=[...document.querySelectorAll('h3')].find(x=>txt(x)==='Product Fokus Achievement'),section=h?.closest('section');if(!section)return;const grid=[...section.querySelectorAll('div')].find(d=>d.querySelectorAll(':scope > label').length===4);if(!grid)return;const labels=[...grid.querySelectorAll(':scope > label')],year=labels.find(l=>txt(l).startsWith('Tahun')),from=labels.find(l=>txt(l).startsWith('Dari')),to=labels.find(l=>txt(l).startsWith('Sampai')),week=labels.find(l=>txt(l).startsWith('Week'));if(!year||!week||!from||!to)return;
    grid.className='grid gap-3 md:grid-cols-2';year.style.display='block';week.style.display='block';from.style.display='block';to.style.display='block';year.style.order='1';week.style.order='2';from.style.order='3';to.style.order='4';
    const weekSelect=week.querySelector('select');if(weekSelect){const all=weekSelect.querySelector('option[value="ALL"]');if(all)all.textContent='Pilih Week';if(!week.dataset.j1WeekToggle){week.dataset.j1WeekToggle='1';const title=week.querySelector('span');if(title)title.textContent='Filter Week';const box=document.createElement('div');box.className='mb-2 flex items-center justify-between rounded-xl border bg-slate-50 px-3 py-2';const label=document.createElement('span');label.className='text-xs font-black uppercase text-slate-500';label.textContent='Gunakan Week';const btn=document.createElement('button');btn.type='button';btn.className='rounded-full bg-slate-200 px-3 py-1 text-xs font-black text-slate-600';btn.textContent='OFF';btn.dataset.on='0';const sync=()=>{const on=btn.dataset.on==='1';weekSelect.disabled=!on;from.querySelector('input').disabled=on;to.querySelector('input').disabled=on;btn.textContent=on?'ON':'OFF'};btn.onclick=()=>{btn.dataset.on=btn.dataset.on==='1'?'0':'1';sync()};box.append(label,btn);week.insertBefore(box,weekSelect);sync()}}
  }
  async function staff(){const root=byHeading('Staff Performance');if(!root)return;const table=root.querySelector('table');if(!table)return;const rows=await audit(currentPeriod(root)),map=mapRows(rows),heads=[...table.querySelectorAll('thead th')];if(!heads.some(h=>txt(h)==='AirPods')){const w=heads.find(h=>txt(h)==='Apple Watch');if(w){const th=document.createElement('th');th.className=w.className;th.textContent='AirPods';w.insertAdjacentElement('afterend',th)}}for(const tr of table.querySelectorAll('tbody tr')){const cells=[...tr.querySelectorAll('td')];if(cells.length<8)continue;const r=map.get(`${txt(cells[1])}|${txt(cells[0]).toUpperCase()}`),watch=cells[7];let air=[...tr.querySelectorAll('td')].find(c=>c.dataset.j1Airpods==='1');if(!air){air=document.createElement('td');air.className=watch.className;air.dataset.j1Airpods='1';watch.insertAdjacentElement('afterend',air)}air.textContent=number.format(r?.airpods||0)}}
  async function incentive(){const root=byHeading('Est Incentive');if(!root)return;const table=root.querySelector('table');if(!table)return;const rows=await audit(currentPeriod(root)),map=mapRows(rows);for(const tr of table.querySelectorAll('tbody tr')){const c=[...tr.querySelectorAll('td')];if(c.length<9)continue;const r=map.get(`${txt(c[1])}|${txt(c[0]).toUpperCase()}`);if(!r)continue;[r.incentive.iphone,r.incentive.macbook,r.incentive.ipad,r.incentive.watch,r.incentive.accessories,r.incentive.qoala,r.incentive.total].forEach((v,i)=>{if(c[i+2])c[i+2].textContent=money.format(v||0)})}}
  async function savedFeedback(date){if(feedbackCache.has(date))return feedbackCache.get(date);const rows=await fetch(`/api/jakarta1-feedback?date=${encodeURIComponent(date)}`,{cache:'no-store'}).then(r=>r.json()).then(j=>j.rows||[]).catch(()=>[]);const set=new Set(rows.map(r=>String(r.store||'').toUpperCase()));feedbackCache.set(date,set);return set}
  function makeFeedbackForm(card,badge,date,code,sent){let form=card.querySelector('[data-j1-feedback-form="1"]');if(form){form.scrollIntoView({behavior:'smooth',block:'nearest'});form.querySelector('textarea')?.focus();return}form=document.createElement('div');form.dataset.j1FeedbackForm='1';form.className='mt-4 grid gap-3 border-t pt-4';const select=document.createElement('select');select.className='h-11 rounded-xl border bg-white px-3 font-bold';select.innerHTML='<option value="">Pilih Kategori *</option><option value="external">Faktor External</option><option value="internal">Internal</option><option value="bnpl">Promo BNPL</option><option value="stock">Ketersediaan Stok</option>';const area=document.createElement('textarea');area.className='min-h-24 rounded-xl border bg-white px-3 py-3';area.placeholder='Tuliskan reason / issue yang terjadi...';const btn=document.createElement('button');btn.className='h-11 rounded-xl bg-blue-600 px-4 font-bold text-white';btn.textContent='Simpan Feedback';const err=document.createElement('p');err.className='text-xs font-bold text-rose-600';btn.onclick=async e=>{e.stopPropagation();err.textContent='';const category=select.value,feedback=area.value.trim();if(!category||!feedback){err.textContent='Kategori dan Reason wajib diisi.';return}btn.disabled=true;btn.textContent='Menyimpan...';try{const r=await fetch('/api/jakarta1-feedback',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({date,store:code,category,feedback})});const j=await r.json();if(!r.ok&&r.status!==409)throw new Error(j.error||'Gagal menyimpan feedback');sent.add(code);feedbackCache.set(date,sent);badge.textContent='Feedback Submitted';badge.className='rounded-full px-3 py-1 text-xs font-black bg-blue-100 text-blue-700';card.classList.remove('cursor-pointer','hover:border-blue-300','hover:shadow-md');form.remove()}catch(e){err.textContent=e.message||'Gagal menyimpan feedback';btn.disabled=false;btn.textContent='Simpan Feedback'}};for(const el of [select,area])el.addEventListener('click',e=>e.stopPropagation());form.append(select,area,btn,err);card.append(form);form.scrollIntoView({behavior:'smooth',block:'nearest'});area.focus()}
  async function reasonDaily(){const root=byHeading('Reason Daily');if(!root)return;const dateInput=root.querySelector('input[type="date"]');const date=dateInput?.value;if(!date)return;const sent=await savedFeedback(date);const cards=[...root.querySelectorAll('div.rounded-2xl.border.bg-white')];for(const card of cards){const badge=[...card.querySelectorAll('span')].find(s=>/Need Feedback|Achieved|Feedback Submitted/.test(txt(s)));const store=card.querySelector('b');if(!badge||!store)continue;const code=txt(store).toUpperCase(),need=txt(badge)==='Need Feedback';if(sent.has(code)){badge.textContent='Feedback Submitted';badge.className='rounded-full px-3 py-1 text-xs font-black bg-blue-100 text-blue-700';card.onclick=null;continue}if(!need){card.onclick=null;continue}card.classList.add('cursor-pointer','hover:border-blue-300','hover:shadow-md','transition');badge.classList.add('cursor-pointer');badge.title='Klik untuk isi feedback';card.onclick=e=>{if(e.target.closest('select,textarea,button,[data-j1-feedback-form="1"]'))return;makeFeedbackForm(card,badge,date,code,sent)};badge.onclick=e=>{e.stopPropagation();makeFeedbackForm(card,badge,date,code,sent)}}}
  let raf=0;const run=()=>{raf=0;growth();productFocus();void staff();void incentive();void reasonDaily()};const schedule=()=>{if(!raf)raf=requestAnimationFrame(run)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run,{once:true});else run();new MutationObserver(schedule).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('change',()=>{cache.clear();feedbackCache.clear();schedule()},true);
})();