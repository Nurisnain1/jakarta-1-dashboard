"use client";
import {useEffect,useMemo,useState} from "react";
import {createPortal} from "react-dom";
import {ChevronDown,Target} from "lucide-react";
type Section="lob"|"product"|"vas";type Item={name:string;actual:number;target:number|null;brand?:string;product?:string;category?:string};type Store={code:string;name:string};type Actual={lob:Record<string,number>;products:Record<string,number>;vas:Record<string,number>};type StoreData={info:Store;actual:Actual;targets:Record<string,number>};type Payload={period:string;stores:Store[];summary:{target:number;actual:number};lob:Item[];products:Item[];vas:Item[];storeBreakdown:Array<Store&{target:number;actual:number}>;byStore:Record<string,StoreData>;meta:{targetNote:string;unmatchedRecords:number;processedRecords:number;sources:string[]};error?:string};
const money=new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}),num=new Intl.NumberFormat("id-ID"),months=["Januari","Februari","Maret","April","Mei","Juni","Juli","Agustus","September","Oktober","November","Desember"],paths:Record<Section,string>={lob:"/target-focus/lob",product:"/target-focus/product",vas:"/target-focus/vas"};const metric=(a:number,t:number|null)=>{const target=Number(t)||0,actual=Number(a)||0;return target>0?{pct:actual/target*100,gap:target-actual}:{pct:0,gap:0}};function Progress({a,t}:{a:number;t:number|null}){const m=metric(a,t);return <div className="h-1.5 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-600" style={{width:`${Math.min(100,Math.max(0,m.pct))}%`}}/></div>}function routeSection():Section|null{if(typeof window==="undefined")return null;const p=window.location.pathname;if(p==="/target-focus"||p.startsWith("/target-focus/lob"))return"lob";if(p.startsWith("/target-focus/product"))return"product";if(p.startsWith("/target-focus/vas"))return"vas";return null}
export default function Jakarta1TargetFocus(){const[navHost,setNavHost]=useState<Element|null>(null),[active,setActive]=useState(false),[open,setOpen]=useState(false),[section,setSection]=useState<Section>("lob"),[period,setPeriod]=useState("2026-09"),[store,setStore]=useState("ALL"),[brand,setBrand]=useState("ALL"),[selected,setSelected]=useState(""),[data,setData]=useState<Payload|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState("");
useEffect(()=>{const sync=()=>{const nav=document.querySelector("aside nav");setNavHost(nav);const aside=nav?.closest("aside") as HTMLElement|null;if(aside){aside.style.overflowY="auto";aside.style.maxHeight="100vh"}};const route=()=>{const x=routeSection();setActive(!!x);if(x){setSection(x);setOpen(true)}};sync();route();const o=new MutationObserver(sync);o.observe(document.body,{subtree:true,childList:true});window.addEventListener("popstate",route);return()=>{o.disconnect();window.removeEventListener("popstate",route)}},[]);
useEffect(()=>{if(!active)return;const c=new AbortController();setBusy(true);setError("");fetch(`/api/jakarta1-target-focus?period=${period}`,{signal:c.signal}).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error||"Gagal memuat data");setData(j)}).catch(e=>{if(e.name!=="AbortError"){setData(null);setError(e.message)}}).finally(()=>{if(!c.signal.aborted)setBusy(false)});return()=>c.abort()},[active,period]);
const go=(x:Section)=>{setActive(true);setOpen(true);setSection(x);setBrand("ALL");setSelected("");history.pushState({},"",paths[x]);window.dispatchEvent(new PopStateEvent("popstate"))},close=()=>{setActive(false);setOpen(false);history.pushState({},"","/");window.dispatchEvent(new PopStateEvent("popstate"))};const rows=useMemo<Item[]>(()=>{if(!data)return[];if(store==="ALL")return section==="lob"?data.lob:section==="product"?data.products:data.vas;const d=data.byStore[store];if(!d)return[];const source=section==="lob"?d.actual.lob:section==="product"?d.actual.products:d.actual.vas;return Object.entries(source).map(([name,actual])=>{const all=section==="product"?data.products.find(x=>x.product===name):undefined;return{name,product:section==="product"?name:undefined,brand:all?.brand,category:all?.category,actual,target:d.targets[`${section}|${name.toUpperCase()}`]??null}})},[data,store,section]);const brands=useMemo(()=>Array.from(new Set(rows.map(x=>x.brand).filter((x):x is string=>!!x))).sort(),[rows]);const filtered=rows.filter(x=>brand==="ALL"||x.brand===brand);useEffect(()=>{if(filtered.length&&!filtered.some(x=>x.name===selected))setSelected(filtered[0].name);if(!filtered.length)setSelected("")},[section,store,brand,period,data]);const detail=filtered.find(x=>x.name===selected)||filtered[0];const total=filtered.reduce((a,x)=>({target:a.target+(x.target||0),actual:a.actual+x.actual}),{target:0,actual:0}),tm=metric(total.actual,total.target),dm=detail?metric(detail.actual,detail.target):{pct:0,gap:0};const currency=section!=="product",fmt=(v:number)=>currency?money.format(v):`${num.format(v)} Unit`;
const nav=navHost?createPortal(<div className="lg:w-full"><button onClick={()=>{setOpen(v=>!v);if(!active)go("lob")}} className={`flex w-full items-center gap-2 rounded-xl px-3 py-3 text-sm font-bold ${active?"bg-white/20":"text-white/85 hover:bg-white/10"}`}><Target className="size-4"/><span>Target Fokus</span><ChevronDown className={`ml-auto size-3 ${open?"rotate-180":""}`}/></button>{open&&<div className="mt-1 space-y-1 pl-5">{[["lob","LOB Target Fokus"],["product","Product Fokus 3PP"],["vas","VAS Fokus"]].map(([k,l])=><button key={k} onClick={()=>go(k as Section)} className={`w-full rounded-lg px-3 py-2 text-left text-xs font-bold ${active&&section===k?"bg-white/20":"text-white/75 hover:bg-white/10"}`}>{l}</button>)}</div>}</div>,navHost):null;if(!active)return nav;const title=section==="lob"?"LOB Target Fokus":section==="product"?"Product Fokus 3PP":"VAS Fokus";
const content=<section className="mx-auto max-w-[1500px] space-y-4"><div><p className="text-xs font-bold text-slate-400">Jakarta 1 <span className="px-2">›</span> {title}</p><h2 className="mt-2 text-2xl font-black">{title}</h2><p className="text-sm text-slate-500">Monitoring Target Fokus Jakarta 1 berbasis data aktual.</p></div><div className="rounded-2xl border bg-white p-4 shadow-sm"><div className="mb-4 flex gap-2"><button className="rounded-full border px-4 py-2 text-xs font-bold text-slate-500">Week</button><button className="rounded-full bg-blue-600 px-4 py-2 text-xs font-bold text-white">Bulan</button><button className="rounded-full border px-4 py-2 text-xs font-bold text-slate-500">Range Tanggal</button></div><div className="grid gap-3 sm:grid-cols-2"><label><span className="mb-1 block text-[11px] font-black uppercase text-slate-400">Filter Bulan</span><select value={period} onChange={e=>{setPeriod(e.target.value);setStore("ALL")}} className="h-11 w-full rounded-xl border px-3 font-bold">{[2025,2026].flatMap(y=>months.map((m,i)=><option key={`${y}-${i}`} value={`${y}-${String(i+1).padStart(2,"0")}`}>{m} {y}</option>))}</select></label><label><span className="mb-1 block text-[11px] font-black uppercase text-slate-400">Store</span><select value={store} onChange={e=>setStore(e.target.value)} className="h-11 w-full rounded-xl border px-3 font-bold"><option value="ALL">ALL STORE</option>{data?.stores.map(x=><option key={x.code} value={x.code}>{x.name} • {x.code}</option>)}</select></label></div></div>{error?<div className="rounded-2xl border border-rose-200 bg-rose-50 p-5 font-bold text-rose-700">Data gagal dimuat: {error}</div>:busy?<div className="rounded-2xl border bg-white p-10 text-center text-slate-400">Memuat Target Fokus…</div>:data?<><div className="grid gap-3 sm:grid-cols-4">{[["Target",fmt(total.target)],["Achievement",fmt(total.actual)],["Achievement %",`${tm.pct.toFixed(1)}%`],["Gap",fmt(tm.gap)]].map(([l,v])=><div key={l} className="rounded-2xl border bg-white p-4"><p className="text-[11px] font-black uppercase text-slate-400">{l}</p><p className="mt-2 text-xl font-black">{v}</p></div>)}</div><div className="rounded-2xl border bg-white p-4 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3"><div><h3 className="font-black">{section==="product"?"Product Fokus Aktif":section==="lob"?"LOB Fokus Aktif":"VAS Fokus Aktif"}</h3><p className="text-xs text-slate-500">Tap item untuk melihat detail kontribusi.</p></div>{section==="product"&&brands.length>1&&<select value={brand} onChange={e=>setBrand(e.target.value)} className="rounded-xl border px-3 py-2 text-xs font-bold"><option value="ALL">Semua Brand</option>{brands.map(x=><option key={x}>{x}</option>)}</select>}</div><div className="mt-4 flex gap-2 overflow-x-auto pb-2">{filtered.map(x=><button key={x.name} onClick={()=>setSelected(x.name)} className={`whitespace-nowrap rounded-full border px-4 py-2 text-xs font-bold ${detail?.name===x.name?"border-blue-600 bg-blue-600 text-white":"bg-white text-slate-600"}`}>{x.name}</button>)}</div></div>{detail?<div className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-[11px] font-black uppercase tracking-widest text-blue-600">Detail {section==="product"?"Product":section==="lob"?"LOB":"VAS"}</p><h3 className="mt-1 text-xl font-black">{detail.name}</h3>{section==="product"&&<p className="text-xs text-slate-500">{detail.brand||"—"} • {detail.category||"—"}</p>}</div><span className={`rounded-full px-3 py-1 text-[10px] font-black ${detail.target&&dm.pct>=100?"bg-emerald-50 text-emerald-700":"bg-rose-50 text-rose-600"}`}>{detail.target?dm.pct>=100?"Achieved":"Critical":"Target unavailable"}</span></div><div className="mt-5 grid gap-4 sm:grid-cols-4"><div><p className="text-xs text-slate-400">Target</p><p className="text-xl font-black">{detail.target==null?"—":fmt(detail.target)}</p></div><div><p className="text-xs text-slate-400">Achievement</p><p className="text-xl font-black">{fmt(detail.actual)}</p></div><div><p className="text-xs text-slate-400">Achievement %</p><p className="text-xl font-black">{dm.pct.toFixed(1)}%</p></div><div><p className="text-xs text-slate-400">Gap</p><p className="text-xl font-black">{detail.target==null?"—":fmt(dm.gap)}</p></div></div><div className="mt-4"><Progress a={detail.actual} t={detail.target}/></div></div>:<div className="rounded-2xl border bg-white p-8 text-center text-slate-400">Tidak ada data untuk filter ini.</div>}{store==="ALL"&&<div className="rounded-2xl border bg-white p-4"><h3 className="font-black">Store Performance</h3><div className="mt-3 overflow-x-auto"><table className="w-full"><thead><tr>{["Store","Target","Actual","Ach %"].map(x=><th key={x} className="px-3 py-2 text-left text-[11px] font-black uppercase text-slate-400">{x}</th>)}</tr></thead><tbody>{data.storeBreakdown.map(x=>{const m=metric(x.actual,x.target);return <tr key={x.code} className="border-t"><td className="px-3 py-3 text-sm font-bold">{x.name} <span className="text-slate-400">{x.code}</span></td><td className="px-3 py-3 text-sm">{money.format(x.target)}</td><td className="px-3 py-3 text-sm">{money.format(x.actual)}</td><td className="px-3 py-3 text-sm font-bold">{m.pct.toFixed(1)}%</td></tr>})}</tbody></table></div></div>}<div className="rounded-xl border border-amber-200 bg-amber-50 p-3 text-xs text-amber-900">{data.meta.targetNote} • Unmatched mapping: {data.meta.unmatchedRecords}/{data.meta.processedRecords}.</div></>:null}<button onClick={close} className="rounded-xl border bg-white px-4 py-2 text-sm font-bold">Kembali ke dashboard</button></section>;
return <>{nav}<div className="fixed inset-y-0 right-0 z-30 overflow-y-auto bg-slate-50 p-4 pt-20 sm:p-7 lg:left-[240px]">{content}</div></>}
