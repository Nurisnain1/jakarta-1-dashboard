"use client";
import {useEffect,useMemo,useState} from "react";
import {createPortal} from "react-dom";
import {Target} from "lucide-react";
type Item={name:string;actual:number;target:number|null;brand?:string;product?:string};type Payload={period:string;stores:string[];aggregateTargets:{device:number;accessories:number;vas:number};lob:Item[];products:Item[];vas:Item[];byStore:Record<string,{actual:Record<string,any>;target:{device:number;acc:number;vas:number}}>;meta:{targetNote:string};error?:string};
const money=new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}),num=new Intl.NumberFormat("id-ID"),months=["Januari","Februari","Maret","April","Mei","Juni","Juli","Agustus","September","Oktober","November","Desember"],th="whitespace-nowrap px-3 py-3 text-left text-[11px] font-black uppercase tracking-wide text-slate-400",td="whitespace-nowrap px-3 py-3 text-sm";
function metric(a:number,t:number|null){if(t==null||t<=0)return{pct:null,gap:null};return{pct:a/t*100,gap:t-a}}
function Progress({a,t}:{a:number;t:number|null}){const m=metric(a,t);return m.pct==null?<span className="text-xs text-slate-400">Target belum tersedia</span>:<div className="min-w-36"><div className="h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-600" style={{width:`${Math.min(100,Math.max(0,m.pct))}%`}}/></div><span className="mt-1 block text-xs font-bold">{m.pct.toFixed(1)}%</span></div>}
export default function Jakarta1TargetFocus(){const[navHost,setNavHost]=useState<Element|null>(null),[host,setHost]=useState<Element|null>(null),[active,setActive]=useState(false),[section,setSection]=useState<"lob"|"product"|"vas">("lob"),[period,setPeriod]=useState("2026-09"),[store,setStore]=useState("ALL"),[data,setData]=useState<Payload|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState("");useEffect(()=>{const sync=()=>{setNavHost(document.querySelector("aside nav"));setHost(document.querySelector("main > div.space-y-5"))};sync();const o=new MutationObserver(sync);o.observe(document.body,{subtree:true,childList:true});return()=>o.disconnect()},[]);useEffect(()=>{if(!active||!host)return;const els=Array.from(host.children) as HTMLElement[];els.forEach(x=>x.style.display="none");return()=>els.forEach(x=>x.style.display="")},[active,host]);useEffect(()=>{if(!active)return;setBusy(true);setError("");fetch(`/api/jakarta1-target-focus?period=${period}`).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error||"Gagal memuat data");setData(j)}).catch(e=>setError(e.message)).finally(()=>setBusy(false))},[active,period]);const rows=useMemo(()=>{if(!data)return[];if(store==="ALL")return section==="lob"?data.lob:section==="product"?data.products:data.vas;const s=data.byStore[store];if(!s)return[];if(section==="lob")return[{name:"iPhone",actual:s.actual.iphone,target:null},{name:"MacBook",actual:s.actual.macbook,target:null},{name:"iPad",actual:s.actual.ipad,target:null},{name:"Watch",actual:s.actual.watch,target:null},{name:"AirPods",actual:s.actual.airpods,target:null},{name:"Accessories",actual:s.actual.accessories,target:s.target.acc}];if(section==="product")return Object.entries(s.actual.products||{}).map(([name,actual])=>({name,brand:name,product:name,actual:Number(actual),target:null}));return[{name:"Qoala",actual:s.actual.qoala,target:null},{name:"Telkomsel",actual:s.actual.telkomsel,target:null},{name:"XL",actual:s.actual.xl,target:null},{name:"Indosat",actual:s.actual.indosat,target:null}]},[data,store,section]);const nav=navHost?createPortal(<button onClick={()=>setActive(true)} className={`flex items-center justify-center gap-2 rounded-xl px-3 py-3 text-xs font-bold lg:w-full lg:justify-start lg:text-sm ${active?"bg-white/20":"text-white/85 hover:bg-white/10"}`}><Target className="size-4"/><span className="hidden sm:inline">Target Fokus</span></button>,navHost):null;if(!active||!host)return nav;return <>{nav}{createPortal(<section className="space-y-5"><div><p className="text-xs font-black uppercase tracking-[.18em] text-blue-600">Jakarta 1</p><h2 className="mt-2 text-3xl font-black">Target Fokus</h2><p className="mt-1 text-sm text-slate-500">Monitoring LOB, Product Fokus 3PP dan VAS dari source existing Jakarta 1.</p></div><div className="grid gap-3 md:grid-cols-2"><label><span className="mb-1 block text-xs font-black uppercase text-slate-400">Periode</span><select value={period} onChange={e=>setPeriod(e.target.value)} className="h-11 w-full rounded-xl border bg-white px-3 font-bold">{[2025,2026].flatMap(y=>months.map((m,i)=><option key={`${y}-${i}`} value={`${y}-${String(i+1).padStart(2,"0")}`}>{m} {y}</option>))}</select></label><label><span className="mb-1 block text-xs font-black uppercase text-slate-400">Store</span><select value={store} onChange={e=>setStore(e.target.value)} className="h-11 w-full rounded-xl border bg-white px-3 font-bold"><option value="ALL">All Jakarta 1</option>{data?.stores.map(s=><option key={s}>{s}</option>)}</select></label></div><div className="flex flex-wrap gap-2">{[["lob","LOB Target Fokus"],["product","Product Fokus 3PP"],["vas","VAS Fokus"]].map(([k,l])=><button key={k} onClick={()=>setSection(k as any)} className={`rounded-xl px-4 py-2 text-sm font-black ${section===k?"bg-[#086aad] text-white":"border bg-white"}`}>{l}</button>)}</div>{error&&<div className="rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm font-bold text-rose-700">{error}</div>}{data?.meta.targetNote&&<div className="rounded-xl border border-amber-200 bg-amber-50 p-3 text-xs text-amber-800">{data.meta.targetNote}</div>}{busy?<div className="rounded-2xl border bg-white p-8 text-center text-slate-400">Memuat Target Fokus…</div>:<div className="overflow-x-auto rounded-2xl border bg-white shadow-sm"><table className="w-full"><thead><tr>{(section==="product"?["Brand","Product","Target","Actual","Achievement %","Gap","Progress"]:[section==="lob"?"LOB":"VAS","Target","Actual","Achievement %","Gap","Progress"]).map(x=><th key={x} className={th}>{x}</th>)}</tr></thead><tbody>{rows.length?rows.map((r,i)=>{const m=metric(r.actual,r.target),currency=section==="vas"||r.name==="Accessories";return <tr className="border-t" key={`${r.name}-${i}`}>{section==="product"&&<td className={`${td} font-black`}>{r.brand}</td>}<td className={`${td} font-black`}>{section==="product"?r.product:r.name}</td><td className={td}>{r.target==null?"—":currency?money.format(r.target):num.format(r.target)}</td><td className={td}>{currency?money.format(r.actual):num.format(r.actual)}</td><td className={td}>{m.pct==null?"—":`${m.pct.toFixed(1)}%`}</td><td className={td}>{m.gap==null?"—":currency?money.format(m.gap):num.format(m.gap)}</td><td className={td}><Progress a={r.actual} t={r.target}/></td></tr>}):<tr><td colSpan={7} className="p-8 text-center text-slate-400">Tidak ada data untuk filter ini.</td></tr>}</tbody></table></div>}<button onClick={()=>setActive(false)} className="rounded-xl border bg-white px-4 py-2 text-sm font-bold">Kembali ke dashboard</button></section>,host)}</>}
